# trakt.tv credentials

Authentication has changed with the migration to
[httr2::httr2](https://httr2.r-lib.org/reference/httr2-package.html), so
this function is just a placeholder that shows information about
available credentials.

## Usage

``` r
trakt_credentials()
```
