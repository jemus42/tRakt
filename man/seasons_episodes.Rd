% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasons_episodes.R
\name{seasons_episodes}
\alias{seasons_episodes}
\title{Get a season of a show}
\source{
\code{seasons_episodes()} wraps endpoint \href{https://trakt.docs.apiary.io/#reference/seasons/episodes/get-all-episodes-for-a-single-season}{/shows/:id/seasons/:season?translations=}.
}
\usage{
seasons_episodes(id, seasons = 1L, extended = "min")
}
\arguments{
\item{id}{\code{character(1)}: The ID of the item requested. Preferably the
\code{trakt} ID (e.g. \code{1429}). Other options are the trakt.tv \code{slug} (e.g. \code{"the-wire"})
or \code{imdb} ID (e.g. \code{"tt0306414"}).
Can also be of length greater than 1, in which case the function is called on all
\code{id} values separately and the result is combined. See
\code{vignette("tRakt")} for more details.}

\item{seasons}{\code{integer(1) [1L]}: The season(s) to get. Use \code{0} for specials.}

\item{extended}{\code{character}: Level of detail for the API response.
\itemize{
\item \code{"min"} (default): Minimal info (title, year, IDs). Omits the \code{extended} query param.
\item \code{"full"}: Complete info including overview, ratings, runtime, etc.
\item \code{"images"}: Minimal info plus image URLs (returned as a list-column).
\item \code{"full,images"}: Complete info plus images.
\item \code{"metadata"}: Collection endpoints only; adds video/audio metadata.
}

Multiple values can be combined as a comma-separated string (e.g. \code{"full,images"})
or a character vector (e.g. \code{c("full", "images")}).}
}
\value{
A \link[tibble:tibble-package]{tibble()}. If the function has a \code{limit} parameter
(defaulting to \code{10}), this will be the (maximum) number of rows of the \code{tibble}.
If there are no results (or the API is unreachable), an empty \code{tibble()} is returned.
}
\description{
Similar to \link{seasons_summary}, but this function returns full data for
a single season, i.e. all the episodes of the season
}
\note{
If you want to quickly gather episode data of all available seasons,
see \link{seasons_summary} and use the \code{episodes = TRUE} parameter.
}
\examples{
\dontrun{
seasons_episodes("breaking-bad", 1)

# Including all episode data:
seasons_episodes("breaking-bad", 1, extended = "full")
}
}
\seealso{
Other season data: 
\code{\link{media_comments}},
\code{\link{media_lists}},
\code{\link{media_people}},
\code{\link{media_ratings}()},
\code{\link{media_stats}()},
\code{\link{seasons_season}()},
\code{\link{seasons_summary}()}

Other episode data: 
\code{\link{episodes_summary}()},
\code{\link{media_comments}},
\code{\link{media_lists}},
\code{\link{media_people}},
\code{\link{media_ratings}()},
\code{\link{media_stats}()},
\code{\link{media_translations}},
\code{\link{media_watching}},
\code{\link{seasons_summary}()},
\code{\link{shows_next_episode}()}
}
\concept{episode data}
\concept{season data}
