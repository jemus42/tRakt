% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_list_comments.R
\name{user_list_comments}
\alias{user_list_comments}
\title{Get comments on a user-created list}
\source{
\code{user_list_comments()} wraps endpoint \href{https://trakt.docs.apiary.io/#reference/users/list-comments/get-all-list-comments}{/users/:id/lists/:list_id/comments/:sort}.
}
\usage{
user_list_comments(
  user = "me",
  list_id,
  sort = c("newest", "oldest", "likes", "replies"),
  extended = "min"
)
}
\arguments{
\item{user}{\code{character(1)}: Target username (or \code{slug}). Defaults to
\code{"me"}, the OAuth user.
Can also be of length greater than 1, in which case the function is called on all
\code{user} values separately and the result is combined.}

\item{list_id}{The list identifier, either \code{trakt} ID or \code{slug} of the list.
Can be optained via the website (URL \code{slug}) or e.g. \link{user_lists}.}

\item{sort}{\code{character(1) ["newest"]}: Comment sort order, one of
"newest", "oldest", "likes" or "replies".}

\item{extended}{\code{character}: Level of detail for the API response.
\itemize{
\item \code{"min"} (default): Minimal info (title, year, IDs). Omits the \code{extended} query param.
\item \code{"full"}: Complete info including overview, ratings, runtime, etc.
\item \code{"images"}: Minimal info plus image URLs (returned as a list-column).
\item \code{"full,images"}: Complete info plus images.
\item \code{"metadata"}: Collection endpoints only; adds video/audio metadata.
}

Multiple values can be combined as a comma-separated string (e.g. \code{"full,images"})
or a character vector (e.g. \code{c("full", "images")}).}
}
\value{
A \link[tibble:tibble-package]{tibble()}. If the function has a \code{limit} parameter
(defaulting to \code{10}), this will be the (maximum) number of rows of the \code{tibble}.
If there are no results (or the API is unreachable), an empty \code{tibble()} is returned.
}
\description{
Get comments on a user-created list
}
\examples{
\dontrun{
user_list_comments("donxy", "1248149")
}
}
\seealso{
Other comment methods: 
\code{\link{comments_comment}()},
\code{\link{comments_trending}()},
\code{\link{comments_updates}()},
\code{\link{media_comments}},
\code{\link{user_comments}()}

Other list methods: 
\code{\link{lists_popular}()},
\code{\link{media_lists}},
\code{\link{user_list}()},
\code{\link{user_list_items}()},
\code{\link{user_lists}()}
}
\concept{comment methods}
\concept{list methods}
