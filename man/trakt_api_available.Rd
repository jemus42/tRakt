% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-requests.R
\name{trakt_api_available}
\alias{trakt_api_available}
\title{Check whether the trakt.tv API is reachable}
\usage{
trakt_api_available()
}
\value{
\code{logical(1)}: \code{TRUE} if the API responds successfully, else \code{FALSE}.
}
\description{
A lightweight connectivity check that performs a single, small request to a
stable endpoint with a short timeout. It is primarily used to guard runnable
documentation examples (via roxygen2's \verb{@examplesIf}) so they are executed
when the API is available but quietly skipped when it is not — avoiding
spurious failures from transient API outages or a missing internet
connection.
}
\details{
This function never throws: it returns \code{FALSE} on any error (offline,
timeout, non-success status, ...) and does not retry.
}
\examples{
# Returns TRUE or FALSE, never errors:
trakt_api_available()
}
\seealso{
Other API-basics:
\code{\link[=trakt_get]{trakt_get()}}
}
\concept{API-basics}
