% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic-lists.R
\name{updated_media}
\alias{updated_media}
\alias{movies_updates}
\alias{shows_updates}
\title{Recently updated media}
\source{
\code{movies_updates()} wraps endpoint \href{https://trakt.docs.apiary.io/#reference/movies/updates/get-recently-updated-movies}{/movies/updates/:start_date}.

\code{shows_updates()} wraps endpoint \href{https://trakt.docs.apiary.io/#reference/shows/updates/get-recently-updated-shows}{/shows/updates/:start_date}.
}
\usage{
movies_updates(limit = 10, extended = "min", start_date = Sys.Date() - 1)

shows_updates(limit = 10, extended = "min", start_date = Sys.Date() - 1)
}
\arguments{
\item{limit}{\code{integer(1) [10L]}: Number of items to return. Must be greater
than \code{0} and will be coerced via \code{as.integer()}.}

\item{extended}{\code{character}: Level of detail for the API response.
\itemize{
\item \code{"min"} (default): Minimal info (title, year, IDs). Omits the \code{extended} query param.
\item \code{"full"}: Complete info including overview, ratings, runtime, etc.
\item \code{"images"}: Minimal info plus image URLs (returned as a list-column).
\item \code{"full,images"}: Complete info plus images.
\item \code{"metadata"}: Collection endpoints only; adds video/audio metadata.
}

Multiple values can be combined as a comma-separated string (e.g. \code{"full,images"})
or a character vector (e.g. \code{c("full", "images")}).}

\item{start_date}{\code{Date | character(1)}: Return items updated since this
date. Defaults to yesterday. The trakt.tv API only accepts dates up to
\strong{30 days} in the past; older dates return no results (a warning is
emitted).}
}
\value{
A \link[tibble:tibble-package]{tibble()}. If the function has a \code{limit} parameter
(defaulting to \code{10}), this will be the (maximum) number of rows of the \code{tibble}.
If there are no results (or the API is unreachable), an empty \code{tibble()} is returned.
}
\description{
Return movies or shows that were updated on trakt.tv since \code{start_date}.
Handy for keeping a local cache in sync: store the most recent \code{updated_at}
you have seen and poll for anything newer.
}
\note{
Unlike the other dynamic lists, the updates endpoints do not support
the \code{filters} argument.
}
\section{The Dynamic Lists on trakt.tv}{


These functions access the automatically updated lists provided by trakt.tv.
Each function comes in two flavors: Shows or movies. The following descriptions
are adapted directly from the
\href{https://trakt.docs.apiary.io/#reference/movies/popular/get-popular-movies}{API reference}.
\itemize{
\item \link[=popular_media]{Popular}: Popularity is calculated using the rating percentage and the number of
ratings.
\item \link[=trending_media]{Trending}: Returns all movies/shows being watched right now.
Movies/shows with the most users are returned first.
\item \link[=played_media]{Played}: Returns the most played (a single user can watch multiple times)
movies/shows in the specified time \code{period}.
\item \link[=watched_media]{Watched}: Returns the most watched (unique users) movies/shows in the specified
time \code{period}.
\item \link[=collected_media]{Collected}: Returns the most collected (unique users) movies/shows in the
specified time \code{period}.
\item \link[=anticipated_media]{Anticipated}: Returns the most anticipated movies/shows based on the number of
lists a movie/show appears on.
The functions for \strong{Played}, \strong{Watched}, \strong{Collected} and \strong{Played} each return
the same additional variables besides the media information: \code{watcher_count},
\code{play_count}, \code{collected_count}, \code{collector_count}.
}
}

\examples{
\dontshow{if (trakt_api_available()) withAutoprint(\{ # examplesIf}
movies_updates()
shows_updates(start_date = Sys.Date() - 7)
\dontshow{\}) # examplesIf}
}
\seealso{
Other movie data:
\code{\link{anticipated_media}},
\code{\link{collected_media}},
\code{\link{media_aliases}},
\code{\link{media_comments}},
\code{\link{media_lists}},
\code{\link{media_people}},
\code{\link[=media_ratings]{media_ratings()}},
\code{\link[=media_stats]{media_stats()}},
\code{\link{media_translations}},
\code{\link{media_watching}},
\code{\link[=movies_boxoffice]{movies_boxoffice()}},
\code{\link[=movies_related]{movies_related()}},
\code{\link[=movies_releases]{movies_releases()}},
\code{\link[=movies_summary]{movies_summary()}},
\code{\link[=people_media]{people_media()}},
\code{\link{played_media}},
\code{\link{popular_media}},
\code{\link{trending_media}},
\code{\link{watched_media}}

Other dynamic lists:
\code{\link{anticipated_media}},
\code{\link{collected_media}},
\code{\link{filters}},
\code{\link[=lists_popular]{lists_popular()}},
\code{\link{played_media}},
\code{\link{popular_media}},
\code{\link{trending_media}},
\code{\link{watched_media}}

Other show data:
\code{\link{collected_media}},
\code{\link{media_aliases}},
\code{\link{media_comments}},
\code{\link{media_lists}},
\code{\link{media_people}},
\code{\link[=media_ratings]{media_ratings()}},
\code{\link[=media_stats]{media_stats()}},
\code{\link{media_translations}},
\code{\link{media_watching}},
\code{\link[=people_media]{people_media()}},
\code{\link{played_media}},
\code{\link[=shows_next_episode]{shows_next_episode()}},
\code{\link[=shows_related]{shows_related()}},
\code{\link[=shows_summary]{shows_summary()}}
}
\concept{dynamic lists}
\concept{movie data}
\concept{show data}
